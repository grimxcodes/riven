#ifndef TOKEN_H
#define TOKEN_H

typedef enum {

    TOKEN_NUMBER,
    TOKEN_STRING,
    TOKEN_IDENTIFIER,

    TOKEN_PLUS,
    TOKEN_MINUS,
    TOKEN_MULTIPLY,
    TOKEN_DIVIDE,

    TOKEN_ASSIGN,

    TOKEN_LESS,
    TOKEN_GREATER,

    TOKEN_EQUAL_EQUAL,
    TOKEN_NOT_EQUAL,

    TOKEN_INCREMENT,
    TOKEN_DECREMENT,

    TOKEN_AND,
    TOKEN_OR,
    TOKEN_NOT,

    TOKEN_AND_SYMBOL,
    TOKEN_OR_SYMBOL,

    TOKEN_LPAREN,
    TOKEN_RPAREN,

    TOKEN_LBRACE,
    TOKEN_RBRACE,

    TOKEN_LBRACKET,
    TOKEN_RBRACKET,

    TOKEN_COMMA,
    TOKEN_DOT,

    TOKEN_IF,
    TOKEN_ALTIF,
    TOKEN_ELSE,

    TOKEN_FLOW,
    TOKEN_DURING,

    TOKEN_CRAFT,
    TOKEN_RETURNS,

    TOKEN_RIVEN,
    TOKEN_CORE,

    TOKEN_STAMP,
    TOKEN_GRAB,

    TOKEN_CORRECT,
    TOKEN_INCORRECT,
    TOKEN_EMP,

    TOKEN_FIRM,

    TOKEN_RISE,
    TOKEN_DROP,

    TOKEN_FRAME,
    TOKEN_BOOT,
    TOKEN_OPEN,
    TOKEN_HIDDEN,
    TOKEN_SPAWN,

    TOKEN_REC,

    TOKEN_PTR,
    TOKEN_REF,

    TOKEN_RAW,

    TOKEN_FETCH,

    TOKEN_SPARK,
    TOKEN_SYNC,

    TOKEN_FILE,

    TOKEN_INT,
    TOKEN_TXT,
    TOKEN_DNUM,

    TOKEN_CONSISTOF,

    TOKEN_EOF

} TokenType;

typedef struct {

    TokenType type;

    char value[256];

} Token;

#endif
