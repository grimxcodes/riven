#ifndef TOKEN_H
#define TOKEN_H

typedef enum {

    TOKEN_NUMBER,
    TOKEN_STRING,
    TOKEN_IDENTIFIER,

    TOKEN_RIVEN,
    TOKEN_CORE,

    TOKEN_STAMP,
    TOKEN_GRAB,

    TOKEN_CONSISTOF,

    TOKEN_IF,
    TOKEN_ALTIF,
    TOKEN_ELSE,

    TOKEN_FLOW,
    TOKEN_DURING,

    TOKEN_CRAFT,
    TOKEN_RETURNS,

    TOKEN_PLUS,
    TOKEN_MINUS,

    TOKEN_INCREMENT,
    TOKEN_DECREMENT,

    TOKEN_ASSIGN,
    TOKEN_EQUAL_EQUAL,

    TOKEN_LESS,
    TOKEN_GREATER,

    TOKEN_AND,
    TOKEN_OR,
    TOKEN_NOT,

    TOKEN_AND_SYMBOL,
    TOKEN_OR_SYMBOL,
    TOKEN_NOT_SYMBOL,

    TOKEN_LPAREN,
    TOKEN_RPAREN,

    TOKEN_LBRACE,
    TOKEN_RBRACE,

    TOKEN_LBRACKET,
    TOKEN_RBRACKET,

    TOKEN_COMMA,

    TOKEN_EOF

} TokenType;

typedef struct {

    TokenType type;

    char value[1000];

} Token;

#endif
